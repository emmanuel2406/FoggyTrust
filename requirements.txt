# FLTrust demo dependencies
# MXNet 1.x (Gluon API) with GPU support (CUDA 11.2; compatible with CUDA 12.x via driver)
# Use mxnet-cu112 for GPU; base mxnet is CPU-only
mxnet-cu112==1.9.1
# MXNet 1.9.x breaks with NumPy >=1.24 (removed aliases/constants).
numpy==1.23.5
tqdm>=4.0.0
matplotlib>=3.5.0
gluoncv>=0.10.5
